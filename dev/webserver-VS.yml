apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argocd
spec:
  gateways:
    - argocd-gateway
  hosts:
    - "www.webserver.com"
  http:
    - route:
      - destination:
          host: webserver.dev.svc.cluster.local
          port:
            number: 80